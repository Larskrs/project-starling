# Dev — Vite proxies /api and /socket to this host
VITE_API_URL=http://localhost:3000

# Production — absolute origin for API calls (leave empty to use same origin)
VITE_API_BASE_URL=https://cino.no

# Base path if deployed under a subdirectory (e.g. /app/)
VITE_BASE_URL=/
